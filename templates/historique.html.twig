<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Search Results</title>
    <link href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css" rel="stylesheet">
    <style>
        body {
            background: url('https://e1.pxfuel.com/desktop-wallpaper/581/154/desktop-wallpaper-backgrounds-for-login-page-login-page.jpg') no-repeat center center fixed;
            background-size: cover;
            color: #fff;
            font-family: 'Arial', sans-serif;
        }
        .navbar-custom {
            background-color: rgba(0, 0, 0, 0.8);
        }
        .navbar-brand, .nav-link {
            color: #fff;
        }
        .navbar-brand:hover, .nav-link:hover {
            color: #f8f9fa;
        }
        .results-container {
            max-width: 900px;
            padding: 30px;
            background: rgba(0, 0, 0, 0.85);
            border-radius: 12px;
            box-shadow: 0 8px 16px rgba(0, 0, 0, 0.4);
            margin: 50px auto;
        }
        .results-container h1 {
            margin-bottom: 20px;
            font-size: 2rem;
            color: #f8f9fa;
        }
        .results-container .list-group-item {
            background: rgba(255, 255, 255, 0.1);
            border: none;
            border-radius: 8px;
            margin-bottom: 10px;
            color: #fff;
            padding: 15px;
            transition: background 0.3s ease;
        }
        .results-container .list-group-item:hover {
            background: rgba(255, 255, 255, 0.3);
        }
        .results-container .btn-primary {
            border-radius: 20px;
            padding: 8px 15px;
            background-color: #007bff;
            border-color: #007bff;
            transition: background-color 0.3s ease, border-color 0.3s ease;
        }
        .results-container .btn-primary:hover {
            background-color: #0056b3;
            border-color: #004085;
        }
        .no-results {
            text-align: center;
            color: #f8f9fa;
            font-size: 1.2rem;
            margin-top: 20px;
        }
    </style>
</head>
<body>
    <nav class="navbar navbar-expand-lg navbar-custom">
        <a class="navbar-brand" href="#">Recherche du Tutoriel</a>
        <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarNav">
            <ul class="navbar-nav ml-auto">
                <li class="nav-item dropdown">
                    <a class="nav-link dropdown-toggle" href="#" id="userDropdown" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                        {{ user.nom }} ({{ user.userid }})
                    </a>
                    <div class="dropdown-menu dropdown-menu-right" aria-labelledby="userDropdown">
                        <a href="{{ path('user_edit_profile', {id: user.userid}) }}" class="dropdown-item"><i class="fas fa-user-edit"></i> Editer le Profil</a>
                        <a href="{{ path('Connexionuser') }}" class="dropdown-item text-danger"><i class="fas fa-sign-out-alt"></i> Déconnexion</a>
                    </div>
                </li>
            </ul>
        </div>
    </nav>

    <div class="container">
        <div class="results-container">
            <h1>Historique de recherche pour {{ user.nom }}</h1>
            {% if historique is empty %}
                <p class="no-results">Vous n'avez aucun historique de recherche pour le moment.</p>
            {% else %}
                <ul class="list-group">
                    {% for entry in historique %}
                        <li class="list-group-item">
                            <strong>Date :</strong> {{ entry.date|date('d-m-Y H:i:s') }} <br>
                            <strong>Recherche :</strong> {{ entry.text }}
                        </li>
                    {% endfor %}
                </ul>
            {% endif %}
        </div>
    </div>

    <script src="https://code.jquery.com/jquery-3.5.1.slim.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/@popperjs/core@2.5.4/dist/umd/popper.min.js"></script>
    <script src="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/js/bootstrap.min.js"></script>
</body>
</html>
